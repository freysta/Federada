federada.com.br {
    # Backend API
    # Quando acessar /api/*, remove o prefixo e manda para o backend
    handle_path /api/* {
        reverse_proxy api:3000
    }

    # Frontend (React)
    # Qualquer outra rota vai para o site
    handle {
        reverse_proxy web:80
    }
}
